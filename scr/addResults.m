function [ edgeCell1 ] = addResults( tripData,timeZone,pathCell )
% results
inSize=size(tripData,1);
for counter=1:inSize

    %selection
    % tripdistance
    distanceError=tripData(counter,9)-tripData(counter,3)
    if 

end


end

